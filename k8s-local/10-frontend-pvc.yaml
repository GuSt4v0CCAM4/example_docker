apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: frontend-data-pvc
  namespace: coarlumini
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard-rwo
  resources:
    requests:
      storage: 2Gi
